{
  "name": "@skycoin4444/workload-router",
  "version": "1.1.0",
  "description": "Deterministic policy-based workload routing library with caller-supplied route identifiers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "audit": "npm audit --audit-level=high",
    "verify": "npm run lint && npm test && npm run build && npm run audit"
  },
  "keywords": ["skycoin4444", "routing", "policy", "workloads"],
  "author": "SKYCOIN4444",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.2.0"
  }
}
